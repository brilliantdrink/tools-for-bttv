{
  "name": "tools-for-bttv",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "NODE_ENV=watch node build.js"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "blockhash-core": "^0.1.0",
    "path-to-regexp": "^8.2.0"
  }
}
